#include <bits/stdc++.h>
using namespace std;

void baroudy14(){
    int x;
    cin >> x;
    if(x > 0)
        cout << x;
    else
        cout << 0;
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int t = 1;
    //cin >> t;

    while(t--){
        baroudy14();
    }
}